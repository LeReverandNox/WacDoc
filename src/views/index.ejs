<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">

    <link rel="stylesheet" href="/css/style.css">

    <title>WacDoc</title>
</head>
<body>
    <h1>WacDoc</h1>
    <div>
        <h2>Uploaded files</h2>
        <ul>
            <% for (var i = 0; i < files.length; i++) { %>
                <li class="timeline-yellow" >
                    <%= files[i].realName %>
                    <%if (files[i].isWac === true) { %>
                        - <a href="/edit/<%= files[i].uuid %>">Edit</a>
                        - <a href="/export-html/<%= files[i].uuid %>">export HTML</a>
                        - <a href="/export-pdf/<%= files[i].uuid %>">export PDF</a>
                        - <a href="/delete/<%= files[i].uuid %>">Delete</a>
                    <% } %>
                    <%if (files[i].isDownloadable === true) { %>
                        - <a href="/download/<%= files[i].uuid %>">Download</a>
                    <% } %>
                </li>
            <% } %>
        </ul>
    </div>
    <div>
        <h2>Create a .mywac file !</h2>
        <form action="/create" method="POST">
            <label for="filename">Name (including extension)</label>
            <input type="text" name="filename" id="filename">
            <input type="submit" value="Create">
        </form>
    </div>
</body>
</html>